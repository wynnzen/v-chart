import legend from "./legend.config";
import series from "./series.config";
import title from "./title.config";
import tooltip from "./tooltip.config";

const options = {
  title,
  tooltip,
  series,
  legend,
};

export { title, tooltip, series, legend };

export default options;
